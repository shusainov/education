package calc;

import FlaNium.WinAPI.DesktopElement;
import org.openqa.selenium.WebElement;

public class myElement extends DesktopElement {
    protected myElement(WebElement element) {
        super(element);
    }
}
